<?xml version="1.0" encoding="UTF-8"?>


<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bitcamp.petcare.matching.mapper.SearchMapper">

	<select id="getPsList" resultType="com.bitcamp.petcare.matching.domain.PetSitterDTO">
		SELECT /*+ index_desc(tbl_petsitter_profile) */ *
		FROM tbl_user
		LEFT OUTER JOIN  tbl_petsitter_profile on tbl_user.user_no 
		                                        = tbl_petsitter_profile.user_no
		WHERE user_classify = 2
	</select>
	
	<select id="getPs" resultType="com.bitcamp.petcare.matching.domain.PetSitterDTO">
		SELECT /*+ index_desc(tbl_petsitter_profile) */ *
        FROM tbl_petsitter_profile WHERE user_no = #{user_no}
	</select>
	
</mapper>