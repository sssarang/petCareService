<?xml version="1.0" encoding="UTF-8"?>


<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bitcamp.petcare.mypage.mapper.CustomerInfoManageMapper">


<select id="readInfo" resultType="com.bitcamp.petcare.mypage.domain.CustomerInfoManageVO">

	SELECT user_nickname "닉네임",
		   user_id "아이디",
		   user_contact "연락처",
		   user_address "주소",
		   user_latitude "위도",
		   user_longitude "경도",
		   user_gender "성별"
	FROM tbl_user	
	WHERE user_no = ${userNo}

</select>

<select id="readPw" resultType="com.bitcamp.petcare.mypage.domain.userPasswordVO">
	
	SELECT user_pw
	FROM tbl_user
	WHERE user_no = ${userNo}

</select>

<update id="updateInfo">
	UPDATE tbl_user u
	SET 
	     u.user_nickname = ${userNickname},
	     u.user_contact = ${userContact},
	     u.user_address = ${userAddress},
	     u.user_latitude = ${userLatitude},
	     u.user_longitude = ${userLongitude}
	     
	WHERE u.user_no = ${userNo}
</update>

</mapper>
