<?xml version="1.0" encoding="UTF-8"?>


<beans:beans 
	xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	<!-- Enables the Spring MVC @Controller programming model -->
	<annotation-driven />

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<resources mapping="/resources/**" location="/resources/" />

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>
	
	<!-- file Upload -->
    <beans:bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <!-- max upload size in bytes -->
        <beans:property name="maxUploadSize" value="10485760" /> <!-- 10MB -->
        <!-- max size of file in memory (in bytes) -->
        <beans:property name="maxInMemorySize" value="1048576" /> <!-- 1MB -->
        <beans:property name="defaultEncoding" value="utf-8"/>
    </beans:bean>
	
	<context:component-scan base-package="com.bitcamp.petcare.common.controller" />
	<context:component-scan base-package="com.bitcamp.petcare.home.controller" />
	<context:component-scan base-package="com.bitcamp.petcare.user.controller" />
	<context:component-scan base-package="com.bitcamp.petcare.matching.controller" />
	<context:component-scan base-package="com.bitcamp.petcare.mypage.controller" />
	<context:component-scan base-package="com.bitcamp.petcare.admin.controller" />
	
	
	<!-- 뷰 컨트롤러 추가 -->
	<view-controller path="/user/findPassword" view-name="user/findPassword" status-code="200"/>
	<view-controller path="/user/joinPage" view-name="user/joinPage" status-code="200"/>
	<view-controller path="/user/loginPage" view-name="user/loginPage" status-code="200"/>
	<view-controller path="/user/articleCheckPage" view-name="user/articleCheckPage" status-code="200"/>

	<beans:bean id="loginInterceptor" class="com.bitcamp.petcare.user.interceptor.LoginInterceptor"/>
	
	<beans:bean id="authInterceptor" class="com.bitcamp.petcare.user.interceptor.AuthInterceptor"/>
	
	<interceptors>
		<interceptor>
			<mapping path="/user/login" />
	
			<beans:ref bean="loginInterceptor" />
		</interceptor> 

		<interceptor>
			<mapping path="/admin/*" />
			<mapping path="/search/matching" />

			<beans:ref bean="authInterceptor"/>
		</interceptor>
		
	</interceptors>
	
</beans:beans>
